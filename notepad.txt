<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Notepad Sederhana</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
        }

        .container {
            max-width: 1000px;
            margin: 0 auto;
            background: rgba(255, 255, 255, 0.95);
            border-radius: 15px;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            backdrop-filter: blur(10px);
        }

        .header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 20px;
            text-align: center;
        }

        .header h1 {
            font-size: 28px;
            margin-bottom: 10px;
        }

        .header p {
            opacity: 0.9;
            font-size: 14px;
        }

        .toolbar {
            padding: 15px 20px;
            background: #f8f9fa;
            border-bottom: 1px solid #e9ecef;
            display: flex;
            gap: 10px;
            flex-wrap: wrap;
            align-items: center;
        }

        .btn {
            padding: 8px 16px;
            border: none;
            border-radius: 6px;
            cursor: pointer;
            font-size: 14px;
            transition: all 0.2s ease;
            font-weight: 500;
        }

        .btn-primary {
            background: #007bff;
            color: white;
        }

        .btn-primary:hover {
            background: #0056b3;
            transform: translateY(-1px);
        }

        .btn-secondary {
            background: #6c757d;
            color: white;
        }

        .btn-secondary:hover {
            background: #545b62;
            transform: translateY(-1px);
        }

        .btn-success {
            background: #28a745;
            color: white;
        }

        .btn-success:hover {
            background: #1e7e34;
            transform: translateY(-1px);
        }

        .btn-warning {
            background: #ffc107;
            color: #212529;
        }

        .btn-warning:hover {
            background: #e0a800;
            transform: translateY(-1px);
        }

        .editor-area {
            padding: 20px;
        }

        #notepad {
            width: 100%;
            height: 500px;
            border: 2px solid #e9ecef;
            border-radius: 8px;
            padding: 15px;
            font-size: 14px;
            font-family: 'Consolas', 'Monaco', 'Courier New', monospace;
            line-height: 1.6;
            resize: vertical;
            outline: none;
            transition: border-color 0.3s ease;
        }

        #notepad:focus {
            border-color: #667eea;
            box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
        }

        .stats {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px 20px;
            background: #f8f9fa;
            border-top: 1px solid #e9ecef;
            font-size: 12px;
            color: #6c757d;
        }

        .file-input {
            display: none;
        }

        .filename-input {
            padding: 6px 10px;
            border: 1px solid #ddd;
            border-radius: 4px;
            font-size: 14px;
            margin-right: 10px;
        }

        @media (max-width: 768px) {
            .toolbar {
                flex-direction: column;
                align-items: stretch;
            }
            
            .btn {
                margin-bottom: 5px;
            }
            
            #notepad {
                height: 400px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>📝 Notepad Sederhana</h1>
            <p>Editor teks sederhana dengan fitur menyimpan dan membuka file</p>
        </div>
        
        <div class="toolbar">
            <input type="file" id="fileInput" class="file-input" accept=".txt">
            <button class="btn btn-primary" onclick="openFile()">📂 Buka File</button>
            
            <input type="text" id="filenameInput" class="filename-input" placeholder="nama_file.txt" value="catatan_saya.txt">
            <button class="btn btn-success" onclick="saveFile()">💾 Simpan File</button>
            
            <button class="btn btn-warning" onclick="clearText()">🗑️ Hapus Semua</button>
            <button class="btn btn-secondary" onclick="copyText()">📋 Copy</button>
        </div>
        
        <div class="editor-area">
            <textarea id="notepad" placeholder="Mulai menulis catatan Anda di sini...

Anda dapat:
- Menulis teks apapun
- Menyimpan sebagai file .txt
- Membuka file .txt yang sudah ada
- Menyalin teks ke clipboard

Selamat menulis! ✨"></textarea>
        </div>
        
        <div class="stats">
            <div>
                <span id="charCount">0</span> karakter | 
                <span id="wordCount">0</span> kata | 
                <span id="lineCount">1</span> baris
            </div>
            <div id="lastSaved">Belum pernah disimpan</div>
        </div>
    </div>

    <script>
        const notepad = document.getElementById('notepad');
        const charCount = document.getElementById('charCount');
        const wordCount = document.getElementById('wordCount');
        const lineCount = document.getElementById('lineCount');
        const lastSaved = document.getElementById('lastSaved');
        const fileInput = document.getElementById('fileInput');
        const filenameInput = document.getElementById('filenameInput');

        // Update statistik saat mengetik
        notepad.addEventListener('input', updateStats);

        function updateStats() {
            const text = notepad.value;
            
            // Hitung karakter
            charCount.textContent = text.length;
            
            // Hitung kata
            const words = text.trim().split(/\s+/).filter(word => word.length > 0);
            wordCount.textContent = words.length;
            
            // Hitung baris
            lineCount.textContent = text.split('\n').length;
        }

        // Fungsi untuk menyimpan file
        function saveFile() {
            const text = notepad.value;
            const filename = filenameInput.value.trim() || 'catatan.txt';
            
            const blob = new Blob([text], { type: 'text/plain' });
            const url = URL.createObjectURL(blob);
            
            const a = document.createElement('a');
            a.href = url;
            a.download = filename;
            document.body.appendChild(a);
            a.click();
            document.body.removeChild(a);
            URL.revokeObjectURL(url);
            
            // Update status
            const now = new Date();
            lastSaved.textContent = `Terakhir disimpan: ${now.toLocaleString('id-ID')}`;
        }

        // Fungsi untuk membuka file
        function openFile() {
            fileInput.click();
        }

        fileInput.addEventListener('change', function(e) {
            const file = e.target.files[0];
            if (file && file.type === 'text/plain') {
                const reader = new FileReader();
                reader.onload = function(e) {
                    notepad.value = e.target.result;
                    updateStats();
                    
                    // Update nama file
                    filenameInput.value = file.name;
                    lastSaved.textContent = `File "${file.name}" berhasil dibuka`;
                };
                reader.readAsText(file);
            } else {
                alert('Silakan pilih file .txt');
            }
        });

        // Fungsi untuk menghapus semua teks
        function clearText() {
            if (notepad.value.length > 0) {
                if (confirm('Yakin ingin menghapus semua teks?')) {
                    notepad.value = '';
                    updateStats();
                    lastSaved.textContent = 'Teks telah dihapus';
                }
            }
        }

        // Fungsi untuk copy teks
        function copyText() {
            if (notepad.value.length > 0) {
                notepad.select();
                document.execCommand('copy');
                lastSaved.textContent = 'Teks berhasil disalin ke clipboard';
                
                // Hilangkan seleksi setelah 1 detik
                setTimeout(() => {
                    window.getSelection().removeAllRanges();
                }, 1000);
            } else {
                alert('Tidak ada teks untuk disalin');
            }
        }

        // Shortcut keyboard
        notepad.addEventListener('keydown', function(e) {
            // Ctrl+S untuk simpan
            if (e.ctrlKey && e.key === 's') {
                e.preventDefault();
                saveFile();
            }
            
            // Ctrl+O untuk buka
            if (e.ctrlKey && e.key === 'o') {
                e.preventDefault();
                openFile();
            }
        });

        // Inisialisasi stats
        updateStats();

        // Auto-save ke localStorage setiap 30 detik
        setInterval(() => {
            if (notepad.value.trim()) {
                const autoSaveData = {
                    text: notepad.value,
                    timestamp: new Date().toISOString()
                };
                // Note: localStorage tidak tersedia di Claude.ai, jadi kita skip ini
                console.log('Auto-save attempted:', autoSaveData);
            }
        }, 30000);
    </script>
</body>
</html>